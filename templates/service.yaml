apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookinfo-charts.fullname" . }}
  labels:
    {{- include "bookinfo-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookinfo-charts.selectorLabels" . | nindent 4 }}
